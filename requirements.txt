# Core CV / DL
ultralytics>=8.1.0          # YOLOv8 training & inference
opencv-python>=4.9.0        # Camera calibration, PnP, image ops
opencv-contrib-python>=4.9.0  # Extra modules (aruco etc. if needed later)
numpy>=1.26.0
scipy>=1.12.0
matplotlib>=3.8.0

# Data handling
pandas>=2.1.0
pyyaml>=6.0

# Export / deployment (optional but recommended)
onnx>=1.16.0
onnxruntime>=1.17.0        # Run quantised model locally

# Utilities
tqdm>=4.66.0
pillow>=10.0.0
seaborn>=0.13.0            # Nicer distribution plots
